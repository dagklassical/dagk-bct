---
// src/pages/blockchain/tecnologia.astro
import Layout from '../../layouts/Layout.astro';
import MetadataSection from '../../components/MetadataSection.astro';

const pageTitle = 'Tecnología Blockchain - Dag Klassical BCT';
const pageDescription = 'Proceso de certificación inmutable de Music Cards en la red Polygon. Autenticidad, trazabilidad y valor garantizado.';
---

<Layout title={pageTitle} description={pageDescription}>
  <article class="max-w-4xl mx-auto px-4 py-8">
    <header class="text-center mb-12">
      <h1 class="text-4xl font-advent font-bold mb-4">Certificación Blockchain</h1>
      <p class="text-lg text-slate-600 max-w-2xl mx-auto">
        Cada Music Card es un activo digital único, registrado en la cadena de bloques de Polygon con metadatos inmutables.
      </p>
    </header>

    <!-- Proceso de certificación -->
    <section class="mb-16">
      <h2 class="text-2xl font-advent font-semibold mb-6 text-center">Flujo de Certificación</h2>
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        {['Creación', 'Mastering', 'Certificación', 'Distribución'].map((step, i) => (
          <div key={i} class="text-center">
            <div class="w-12 h-12 rounded-full bg-platinum text-white flex items-center justify-center mx-auto mb-3 font-bold">
              {i + 1}
            </div>
            <h3 class="font-medium">{step}</h3>
          </div>
        ))}
      </div>
    </section>

    <!-- Explicación detallada -->
    <section class="prose prose-slate max-w-none mb-12">
      <h2 class="font-advent text-2xl font-semibold mt-10 mb-4">Cómo funciona</h2>

      <h3>1. Creación de la Obra</h3>
      <p>
        La obra musical es producida, grabada y masterizada por Danilo Álvarez y el equipo de Dag Klassical, con estándares de estudio maestro.
      </p>

      <h3>2. Generación de Metadatos Inmutables</h3>
      <p>
        Antes del minting, se genera un objeto con UUID, timestamp, operador y hash SHA256 del contenido. Este conjunto es irrepetible y público.
      </p>

      <h3>3. Registro en Blockchain (Polygon)</h3>
      <p>
        Se despliega un smart contract (ERC-1155 o ERC-721) en la red Polygon. Cada Music Card recibe un Token ID único, y su hash se registra en la transacción.
      </p>

      <h3>4. Vinculación con el Activo Digital</h3>
      <p>
        La Music Card incluye un código QR que enlaza al audio en FLAC 24-bit/192kHz, partitura digital y página de verificación en Polygonscan.
      </p>
    </section>

    <!-- Ejemplo de transacción -->
    <section class="mb-12 p-6 bg-white border border-platinum rounded-lg">
      <h2 class="font-advent text-2xl font-semibold mb-4">Ejemplo de Transacción</h2>
      <p><strong>Contrato:</strong> <code class="text-xs bg-slate-100 p-1 rounded">0x7a8d...6a5</code></p>
      <p><strong>Token ID:</strong> 1001</p>
      <p><strong>Hash de contenido:</strong> <code class="text-xs bg-slate-100 p-1 rounded">a1b2c3d4e5f6...</code></p>
      <p><strong>Fecha:</strong> 23 de noviembre de 2025, 14:30 UTC</p>
      <p class="mt-2">
        <a href="https://polygonscan.com/token/0x7a8d...?a=1001" target="_blank" class="text-platinum hover:underline font-medium">
          Ver en Polygonscan →
        </a>
      </p>
    </section>

    <!-- Beneficios -->
    <section class="mb-12">
      <h2 class="font-advent text-2xl font-semibold mb-4 text-center">Beneficios para el Coleccionista</h2>
      <div class="grid md:grid-cols-2 gap-6">
        <div class="p-4 border border-platinum rounded-lg">
          <h3 class="font-semibold">✅ Autenticidad Garantizada</h3>
          <p>No puede ser falsificada. El hash es único e irreplicable.</p>
        </div>
        <div class="p-4 border border-platinum rounded-lg">
          <h3 class="font-semibold">📊 Trazabilidad Perpetua</h3>
          <p>Historial de propiedad y origen visible para siempre en blockchain.</p>
        </div>
        <div class="p-4 border border-platinum rounded-lg">
          <h3 class="font-semibold">🎧 Calidad de Estudio Maestro</h3>
          <p>Audio en FLAC 24-bit/192kHz, disponible mediante QR.</p>
        </div>
        <div class="p-4 border border-platinum rounded-lg">
          <h3 class="font-semibold">📈 Valor de Inversión</h3>
          <p>Atado al legado y reconocimiento del artista.</p>
        </div>
      </div>
    </section>

    <!-- Ficha de metadatos inmutables -->
    <div class="text-center">
      <MetadataSection />
    </div>
  </article>
</Layout>